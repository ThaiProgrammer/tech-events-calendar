{
  "dependencies": {
    "flow-bin": "^0.62.0",
    "pegjs": "^0.10.0"
  },
  "private": "true",
  "scripts": {
    "build": "node generate-json.js > public/calendar.json && test -f public/calendar.json"
  }
}
